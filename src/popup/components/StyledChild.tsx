import { styled } from '@mui/material/styles';

/**
 * Inner div for main pages
 */
const StyledChild = styled('div')({
   //    position: 'absolute',
   //    bottom: 0,
});

export default StyledChild;
